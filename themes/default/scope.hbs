{{#> layout}}
	{{{ body }}}

	{{#with cross_refs}}
		<div class="cross-refs">
			{{#each groups}}
				<h4>{{name}}</h4>
				{{#each properties}}
					<h5>{{ name }}</h5>
					<div>{{{ body }}}</div>
				{{/each}}
			{{/each}}
		</div>
	{{/with}}
{{/layout}}